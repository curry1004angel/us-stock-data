# EPS 원천 교체 — 결정과 근거

## SEC가 CI를 막는다는 전제는 틀렸다 (2026-08-23)

`fetch_financials.py` 첫머리에 "SEC가 GitHub Actions·자동화 IP를 모두 403
차단(Akamai)해 야후로 대체했다"고 적혀 있었다. 실측으로 뒤집혔다.

`probe_sec_api.yml`을 Actions에서 돌려 AAPL·MSFT·DELL·BKNG 4종목 모두 HTTP 200을
받았다. **403은 `www.sec.gov`의 `companyfacts.zip` 대량 다운로드에만 해당한다.**
종목별 `data.sec.gov/api/xbrl/companyconcept`는 User-Agent만 붙이면 CI에서 돌아간다.

이 착오 때문에 EPS 원천이 2년 가까이 yfinance에 묶여 있었다.

## 라벨 규칙은 기존 것을 그대로 따른다

`fetch_financials.py:64`가 쓰던 규칙과 같다.

```
year    = 기간 종료일의 달력 연도
quarter = 기간 종료일의 달력 분기  f"{(month-1)//3+1}Q"
```

표본 12종목의 SEC 3개월 구간을 이 규칙으로 라벨링해 기존 값과 대조했더니
116건 일치, 1건 어긋남. 회계연도가 1월에 끝나는 회사(DELL)는 그 해가 다음
달력연도로 붙는데, 기존 데이터가 이미 그렇게 돼 있어 계열이 어긋나지 않는다.

## 최신 제출본만으로는 액면분할이 안 맞는다

처음에는 "같은 기간이 여러 번 공시되면 최신 제출본을 쓴다"로 충분한 줄 알았다.
NVDA FY2024가 2024년 제출본 12.05, 2025년 제출본 1.21로 나와서 소급이 되는 줄 알았다.

전체 계열을 뽑아보니 **10-K는 비교연도 2개만 다시 싣는다.** 연도 Y의 최신 값은
Y+2 제출본 기준이고 그 이후 분할은 반영이 안 된다.

```
NVDA   2021  1.76  제출 2023-02-24   분할 전
       2022  3.91  제출 2024-02-21   분할 전
       2023  0.18  제출 2025-02-26   분할 후(10:1)
```

2022와 2023 사이에 21배 절벽이 생긴다. AAPL·AVGO·WMT도 같은 자리에 절벽이 있다.
**이 절벽은 지금 우리 데이터에도 이미 있다** — NVDA 연간 EPS가
3.91 → 0.176 → 1.21로 기록돼 있어 A 항목이 엔비디아를 "이익이 22배 붕괴한 종목"으로
본다.

## 분할 비율은 yfinance에서 받는다 (추론을 두 번 시도했다가 접었다)

SEC 응답만으로 분할을 역산하려 했다. 같은 기간이 분할 전후로 두 번 공시되면
두 값의 배수가 곧 분할 비율이라는 발상이었다. 두 번 깨졌다.

**첫 시도 — 경계를 모아 곱하기.** 분할이 나면 이후 *모든* 보고서가 비교기간을
다시 싣기 때문에 분할 하나가 여러 제출일에서 감지된다. NVDA는 4:1과 10:1이 각각
네 제출일에서 잡혀 4⁴×10⁴을 곱했고 값이 파괴됐다.

**두 번째 시도 — 제출일마다 배수를 하나만 갖도록 잇기.** `v1 × adj(f1) ==
v2 × adj(f2)`로 최신 제출본부터 거슬러 올라가는 방식. NVDA·DELL·AAPL은 맞았으나
BKNG에서 막혔다. 25:1 분할이 분기 기간에서만 감지되고 연간 값이 실린 10-K에는
간선이 없어, 그 제출본이 분할 전인지 후인지 **EPS만으로는 판별할 수 없었다.**
이웃 제출일에서 배수를 빌려오는 어떤 규칙도 한쪽을 깨뜨렸다.

**결론: yfinance `.splits`를 쓴다.** 정확한 권리락일이 있고, 무엇보다 가격 계열이
같은 소스의 조정가라 EPS와 축이 저절로 맞는다. 종목당 왕복이 하나 늘지만
(실측 약 1초/종목) 추론 버그 한 부류가 통째로 사라진다.

```
NVDA  2021-07-20 ×4, 2024-06-10 ×10
BKNG  2026-04-06 ×25
AAPL  2014-06-09 ×7, 2020-08-31 ×4
```

### 분사는 걸러낸다

yfinance는 분사 조정도 `splits`에 섞는다. DELL의 `1.806`(2018)과 `1.973`(2021
VMware)이 그렇다. SEC는 분사를 중단사업 소급으로 이미 처리하므로 그것까지 곱하면
이중 반영이다. **분모가 작은 분수(1, 2, 4분의 1)에 0.3% 안으로 떨어지는 비율만**
진짜 분할로 본다. 1.998(GOOGL 2:1)은 통과하고 1.973은 탈락한다.

## BKNG에 대한 첫 진단은 틀렸었다

처음에 "yfinance BKNG 2023이 4.7468인데 공시가 118.67이니 25배 오차"라고 봤다.
아니다. **BKNG은 2026-04-06에 25:1 분할을 했고**, yfinance의 4.7468이 분할
조정값으로 맞았다. SEC 쪽이 분할 전 원값이었다.

yfinance EPS가 못 미덥다는 결론 자체는 DELL이 떠받친다 — 분할이 없는데도
9.107482 대 공시 8.79로 3.6% 어긋난다. 가중평균 주식수 대신 기말 주식수를 썼다.

## 중단사업 소급은 받아들인다

후속 제출본은 중단사업을 빼고 계속사업 기준으로 과거를 다시 싣는다.
JNJ 2022가 총계 6.86에서 계속사업 7.93으로 바뀐다. **오닐도 계속사업 기준을
쓰므로 이쪽이 CANSLIM에는 오히려 맞다.** 최신 제출본 우선 원칙을 그대로 둔다.

## 자릿수 오태깅 가드

드물게 회사가 자릿수를 잘못 태깅한다. ULBI가 2019년 33, 2024년 38, 2025년 -35로
실려 있는데 나머지 연도는 0.05~1.57이다. 100배다.

`net_income ÷ eps`로 역산한 주식수가 그 종목 중앙값에서 10배 넘게 벗어나면 버린다.
연도가 4개 이상 있을 때만 적용한다 — 표본이 적으면 중앙값 자체가 못 믿을 값이 된다.
분할을 먼저 조정하므로 분할로 인한 오탐은 이 단계에 오지 않는다.

## 커버리지 구멍은 미계산으로 둔다 (사용자 결정)

표본 60종목 중 46종목(77%)에 us-gaap EPS 태그가 있었다. 없는 12종목의 정체는
폐쇄형 펀드 5, 외국 ADR·IFRS 4, SPAC 1, 실제 미국 사업회사 1(WTTR)이다.
대부분 CANSLIM 대상이 아니다.

사용자가 **yfinance 폴백을 두지 않는 쪽**을 골랐다. 틀린 값이 맞는 값처럼 보이는
것이 이 문제의 핵심이었으므로, 없으면 없는 대로 두고 엔진이 `데이터부족`을 내게 한다.

## 4Q는 SEC가 준 것만 쓴다

SEC 3개월 구간 분포가 1Q 108, 2Q 115, 3Q 111, 4Q 85였다. 회계 4분기를 따로
공시하지 않고 10-K에 연간만 싣는 회사가 있어 4Q가 79% 수준이다.

`연간 − 3분기누적`으로 만들지 않는다. 연간 EPS와 3분기 누적 EPS는 가중평균
주식수가 서로 달라 뺄셈이 성립하지 않는다. `calculate_changes.py`의
`NON_ADDITIVE_ACCOUNTS`에 eps가 들어 있는 것도 같은 이유다.
